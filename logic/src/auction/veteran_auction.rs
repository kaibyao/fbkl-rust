// use chrono::{DateTime, FixedOffset};
// use color_eyre::Result;
// use fbkl_entity::auction;
// use tracing::instrument;

// #[instrument]
// pub async fn start_new_veteran_auction(
//     player_id: i64,
//     start_timestamp: DateTime<FixedOffset>
// ) -> Result<auction::Model> {
//     // Need to first create the FA contract if one doesn't already exist
//     // Then create the auction for it.

//     Ok(auction::Model {})
// }
