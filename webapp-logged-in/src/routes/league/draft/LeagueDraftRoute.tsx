import { FunctionComponent } from 'react';
import { Outlet } from 'react-router-dom';

export const LeagueDraftRoute: FunctionComponent = () => (
  <div>
    Placeholder for Draft route. Outlet: <Outlet />
  </div>
);
